# HANDOFF — サイトリニューアル 2026-08

**最終更新**: 2026-08-14（第11セッション終了時・**全部 push 済み＝本番反映済み**） / まずこのファイルだけ読めば再開できる。

---

## 現在地

2026年8月10日に**フルリニューアルを実施し、Phase 1〜5まで完了**。
その後、第3〜第5セッションでサイト全体の図解に取り組んだが、
**2026-08-11 に図を1枚残らず撤去した**（記事の模式図7枚＋トップ／Aboutの循環図）。
理由は下の「図解 — 全部やめた」に書いた。**そのあと作図の制約を全部外してGPTに依頼し直し、
4記事に8枚を入れ直した（横組み・縦組みとも完備）**。詳細は上の「★ 次にやること」。

トップ／Aboutの循環図は撤去したまま。ヒーローの金型断面図モチーフ
（`die-section.tsx`）は模式図ではなくサイトのモチーフなので対象外。
OGP生成もここから拾っている。

**2026-08-14（第10セッション）に、溜まっていた73コミットを含めて全部 push した。
続く第11セッションで搬送アームの動画を1本入れて、これも push 済み。
本番 https://www.natsuki163.com は現在この内容で動いている。未プッシュはゼロ。**
詳細な経緯は `claudedocs/RENEWAL_PLAN_2026.md`。

---

## ★ 第11セッションでやったこと（2026-08-14）と、次の入口

**未コミット・未プッシュともにゼロ。本番反映済み（`03adb5d`）。**

### 次セッションの入口（すべて本人の判断待ち。CCが先に動かさないこと）

| 件 | 状態 |
|---|---|
| **ダークの文字色 84%** | 92%→84% に落として本番に出した。実機で見て、明るい／暗いと感じたら数値を動かす（1%＝約2.5階調）。`app/globals.css` の `.dark --foreground` |
| **動画の見栄え** | **CSS側の7案は全部だめだった（下記）。残っている道は撮り直しだけ。** transfer-conveyance へ「ズームを詰めて 1920×1080 で出す」を依頼するのが次の一手 |
| **本文フォント BIZ UDPGothic** | 試用中。戻すなら `tailwind.config.ts` の `font-sans` 先頭から `--font-biz-ud` を外すだけ |
| **操作音（既定OFF）** | 実装済み。既定をONにするかは本人判断。`app/components/sound-fx.tsx` の `getSnapshot` |
| **外観検査AI Fig.04 の破線** | 閾値の破線が朱バー上端のラベル「0.323」を横切っている。GPT差し戻し／こちらで数pxずらす／放置 の3択 |
| **About の Approach** | トップと文言が食い違ったまま（トップだけ2回入れ替えたため）。何を置くかは未決 |
| **`claude.svg`** | `public/image/logos/` に置いただけで未使用。使い所があれば入れる |
| **transfer-conveyance 側への書き戻し** | `docs/依頼_Web掲載用動画_20260814.md` が「依頼中」のまま。掲載済みだと向こうに書く必要がある。**作業は transfer-conveyance のセッションで** |

### この回でサイトに入ったもの — 搬送アームの動画1本だけ

CAD自動化の記事（`fusion-ai-automation`）の「動画を1コマンドで生成する仕組み」の
段落の直後。transfer-conveyance で作った掲載用の別版（iso 1アングル／ST1表示／
製品なし／HUDなし）を受け取って組み込んだ。

| 何 | どこ |
|---|---|
| 動画（792×594・6.0秒・831KB） | `public/video/arm-motion.mp4` |
| ポスター（51KB） | `public/video/arm-motion-poster.webp` |
| 組み込み | `app/components/arm-motion.tsx`、登録キー `arm-motion` |
| 元ファイル（無加工で保管） | `~/work/transfer-conveyance/images/20260814/` |

**やった加工は余白の切り落としだけ。** 元の 1280×720 は中身が 676×525（面積の35%）
しか使っておらず、周りが白の余白だった。全243フレームの和集合で外形を測って
792×594 に切り出し、H.264 CRF27・faststart・音声なしで焼き直した。
**中身は拡大も縮小もしていない。** 余白を戻すなら crop を外して流し直すだけ。

**文字は一切載せていない（本人判断）。** 最初は6つの動作名を並べた説明文を付け、
次に「生成した動作確認」というラベルだけ残したが、二段階でどちらも外した。
前の段落が「動画を1コマンドで生成する」と言っているので、動くものが出れば用は足りる。
罫線の囲みも、中に文字が無いと枠だけが残るのでやめた。**足し直さないこと。**
動作の順番は目に見えない読者のために `aria-label` にだけ残してある。

### 動画の見栄え — CSS側で試せることは全部だめだった（2026-08-14・保留）

**「もう少し見栄えをよくしたい」に対して7案を作り、実機で見て「どれも違う」。**
記事は A（いまのまま）で本番に出ている。**同じ案を作り直さないこと。**

| | 案 | 判定 |
|---|---|---|
| A | いま（ダークだけ `brightness-90`） | これが残っている |
| B | 細い枠と内側の余白 | 却下 |
| C | ダークのとき `invert(.93) hue-rotate(180deg)` | 却下。色が変わる（金→クリーム、青→淡い） |
| D | 反転＋枠 | 却下 |
| E | 縁を `mask-image` でぼかす | 却下。**ダークで白く光る** |
| F | 速度 0.7倍 | 却下 |
| G | 白を抜いて地に透かす（WebM alpha） | 却下。**Safari 非対応で iPhone では見えない**。831KB→2.0MB |

**測って分かったこと（次に同じ相談が来たら、ここから始める）**

- **ライトモードは直すところが無い。** 素材の背景が 251,251,251 でページの地が 255。
  すでに同化している。ライトで差が出るのは枠を出す案（B・D）だけ
- **透明筐体の中は背景と完全に同じ 251。** つまり本当に透けている。
  だから白を抜けば地が透ける（G）が、**薄いグレーの面まで半透明扱いになる**ので
  筐体が実物より明るく出る。CSS側のキーイングでは「背景の白」と「とても明るい面」を
  区別できない。**正しくやるならレンダリング側で背景を透明にして出すしかない**
- **PCの高解像度ディスプレイで甘い。** 表示640 CSS px に対し素材792px。2倍で見るなら
  1280px 要る。**スマホ（表示310px）は足りている**
- 斜めに寝た構図なので、余白を切っても左上と右下は空く。これは構図由来で切っても消えない

**残っている道は撮り直しだけ**（transfer-conveyance 側）。効果の大きい順に、
①ズームを詰めて 1920×1080 で出す ②背景を透明で出す ③視点を少し寝かせる
④色を落とす／線画寄りに ⑤接地の影。①だけで「小さい・甘い・角が空く」が同時に片づく。

**比較ページは手元にだけ残してある。** `app/dev/arm-motion`（`/dev/arm-motion` で開く）。
`.gitignore` で git から外してあるので、消すときは `.gitignore` の3行も一緒に消すこと。
WebM alpha を作り直すときは次の1行。

```bash
ffmpeg -i public/video/arm-motion.mp4 -vf "format=rgba,colorkey=0xFBFBFB:0.06:0.05" \
  -c:v libvpx-vp9 -pix_fmt yuva420p -auto-alt-ref 0 -crf 46 -b:v 0 -an public/video/arm-motion-alpha.webm
```

### 動画を入れるときに確かめたこと（次に動画を足すなら同じ手順で）

- **中身の外形は全フレームの和集合で測る。** 1フレームだけ見て切ると、
  アームが動いた先が切れる。`ffmpeg -pix_fmt gray` で流して numpy で `(a<246).any(axis=0)`
- **`cropdetect` は使えなかった。** 背景が真っ白（251,251,251）でも、閾値を下げると
  全面が中身と判定される。素直に画素を読むほうが速い
- **ループの継ぎ目は数字で確かめる。** 末尾コマと先頭コマの平均差（3.01）が
  隣接コマの差（2.4〜2.8）と同程度なら見えない
- **CSPの追加は要らない。** `media-src` が無いので `default-src 'self'` に落ち、
  同一オリジンの動画はそのまま通る
- **`not-prose` は動画にも必要。** prose が中のメディアに上下2emを付ける
- 画面外では `IntersectionObserver` で止め、`prefers-reduced-motion` では
  自動再生せず `controls` を出す（effect 内で DOM を直接触る。state は持たない）

---

## 第10セッションでやったこと（2026-08-14）

**この回の「次の入口」は上の第11セッションの表に引き継いだ。**
（搬送アームの動画は第11セッションで完了。）

### この回でサイトに入ったもの

1. **公開URLの修正** — `sitemap.ts` / `robots.ts` の `baseUrl` が雛形の
   `natsuki-portfolio.vercel.app`（**別人のサイト**）のままだった。`www.natsuki163.com` に修正し、
   `layout.tsx` に `metadataBase` も明示
2. **依存の脆弱性 90件 → 0件** — GitHub の「90件」は**押す前の default branch**（Next 14 時代の
   ロックファイル）に対する数字だった。実際に残っていたのは6件で、うち4件は
   **未使用の依存**（`react-syntax-highlighter` / `dompurify`）が原因。削除で解決。
   破壊的アップグレードは不要だった
3. **リニューアル前のトップの記録** — `claudedocs/archive/pre-renewal-2026-08/`。
   公開が切り替わる直前に**本番から**撮った3枚（PC ライト／ダーク、スマホ）
4. **開発基盤の記事に4つ** — 冒頭のイメージ図／引き出し線つきの端末（メニュー開閉つき）／
   主要画面6枚のスクロール送り／使っている道具（CC・Python・Tailscale）
5. **CAD自動化 Fig.02** — 注記を2行に折り返し（GPTの修正版）
6. **ダークの文字色を 92%→84%**、**Claude Code の公式マーク**

### この回で作った仕組み（次も使う）

| 何 | どこ |
|---|---|
| 実機スクショを重なり分だけ畳んで1本にする | `scripts/build-phone-shot.py` |
| 主要画面を枠に収まる寸法へ揃える（伏字も） | `scripts/build-phone-tour.py` |
| 貼りつきスクロールで送る（見本市v2の 05/06/08/04） | `app/components/work-hub-tour.tsx` |
| iPhone枠つきの画面表示 | `app/components/work-hub-phone.tsx` |

**実機の画面を載せるときの伏字の考え方**（この回で確立した）

- 特定につながるもの（接続先・社名由来のリポジトリ名・図番）は**画像の文字を書き換える**
- **会社メールの件名は書き換えず、ぼかす。** 実在する件名を作り変えると嘘になるため
- report-hub の一覧は**絞り込んだ状態**で撮る。素の一覧には業務の資料名が並ぶ
- 撮る前に必ず全画面を確認する。`stroke_sim_v6r5` は製品名「LOCKING BASE A」が出るので不採用にした

### 踏んだ落とし穴（同じことを繰り返さないため）

- **記事本文の `prose` は中の `img` に上下2emを付ける。** iPhone枠のように画像を積む
  コンポーネントは `not-prose` を付けないと、画面が32pxずつ隙間で割れる（4か所で発生）
- **`mask-image` は要素の箱にかかる。** 箱を端末ぴったりにすると、はみ出している
  側面ボタン（-3px）と影が**帯状に切り落とされる**。箱を左右に広く取る
- **`max-w-[130vw]` のような装飾は横スクロールを生む。** 390px幅で59pxはみ出し、
  縦スクロール中にページが左右へ揺れた。装飾を置いたら必ず
  `document.scrollWidth == clientWidth` を測る
- **Next.js の画像最適化はパスでキャッシュする。** 同じパスのまま中身を差し替えても
  古い画像が出続ける。`rm -rf .next/cache/images` してから dev を再起動する
- **ローカル再現は本番と一致しないことがある。** 旧トップを `2fce507` で再現したら、
  本番より段落間隔が広く全体で6px長かった（Tailwind 4 移行が縦のリズムに効いていた）。
  記録は実物から撮る

---

## 第9セッションでやったこと（2026-08-13〜14）と、次の入口

**未コミットは無い。ビルド・リント・表示確認すべて通っている。** push は未実施（70コミット）。

### 判断待ちで止まっているもの（次セッションの入口）

| 件 | 状態 |
|---|---|
| **本文フォント BIZ UDPGothic** | **試用中。本人が実物を見て採否を決める。** 戻すなら `tailwind.config.ts` の `font-sans` 先頭から `--font-biz-ud` を外すだけ |
| **操作音（既定OFF）** | 実装済み。既定をONにするかは本人判断。`app/components/sound-fx.tsx` の `getSnapshot` |
| **外観検査AI Fig.04 の破線** | 閾値の破線が朱バー上端のラベル「0.323」を横切っている。**直さずに報告した状態**。GPT差し戻し／こちらで数pxずらす／放置 の3択 |
| **About の Approach** | トップと文言が食い違ったまま（トップだけ2回入れ替えたため）。何を置くかは未決 |

### この回でサイトに入ったもの

1. **Approach を本人の新しい文章に差し替え**（AIを手段として領域をつなぎ直す、という宣言）。
   見出し「より良くするために、／AIを学び、使い、確かめる。」は本人が復活を決めたもの。
   **CCの判断で外さないこと**（前回「標語めいて見える」で外した経緯があるが、今回は本人の意思）
2. **球（H-24）を右下の背景へ。** 受領時の viewBox に戻し、中心をコンテンツ枠の右下角に置いて
   上側4分の1だけ覗かせる。切り取りは container の `overflow-hidden`（左と同じ余白が右にも残る）
3. **冒頭イメージ図を2記事に追加**（外観検査AI＝コンベアと撮像装置／CAD自動化＝モニターの中の製図）。
   本文へ右回り込み。**図番は振らない**（本文の Fig 番号を守るため `Schematic` 枠を使わない）
4. **本文フォントを BIZ UDPGothic へ**（試用）
5. **操作音・振動・スクロール音**（project-hub の FX_JS を移植。既定OFF）
6. 細部：ホバーの朱線を番号から4px離した／外観検査AI Fig.01 の仕切りを中央に置いた

### この回で踏んだ罠

- **JSXの `return (` 直後に `{/* */}` は置けない。** パースエラーでページが落ちる。`//` で書く
- **透明度の二重掛けで細線が消える。** `text-foreground/50` に `opacity-60` を重ねると実効30%。
  「図が崩れている」という見え方になる。**濃さの指定は一段だけ**にする
- **`overflow-hidden` を `section` に置くと画面端で切れる。** コンテンツ枠で切りたいなら
  `container` 側に置く（左右の余白が揃う）
- **`captureBeyondViewport: true` はホバー状態を落とす。** `:hover` の確認では外すこと。
  そもそも位置の検証は Runtime.evaluate の実測のほうが速い
- **effect 内の同期 setState は lint に弾かれる**（`react-hooks/set-state-in-effect`）。
  localStorage を読むなら `useSyncExternalStore`
- **露出の小さい図は IntersectionObserver が発火しない。** 4分の1しか見えない図の交差率は
  最大0.25。既定 threshold 0.2 では危うい。`ScrollDraw` に threshold を渡せるようにした

---

## （以下、第7〜8セッションの記録）

### 0. スクロール演出の検討（デモまで作った・未採用）

**本人が Apple の製品ページの「見せ方・演出」を採り入れたいと言っている。**
何度か取り違えたので、確定していることを先に書く。

**指しているのは「1か所の仕掛け」ではない。** スクロールしている間ずっと続く体験全体。
最初「Heroのパンチが下降する」を作って出したが「まったく違う」と言われた（試作は削除済み。
バックアップだけスクラッチに残っている）。

**特に気に入っている2つ**（本人がスクリーンショットで指定）

1. **黒い全画面に巨大な文字。文字は切り抜かれた窓になっていて、中に別の映像が見えている。**
   拡大しながら重なって次の文字へ抜けていく（`background-clip:text` ＋ 背後の映像）
2. **そのあとモノが画面いっぱいに出る**（黒地、大きく、ゆっくり引き寄せる）

**採り入れる範囲は「全部」と言っている。** ただし絵の問題が残る（下記）。

**Apple のページを実測して分かったこと**（推測ではない）

| 項目 | 値 |
|---|---|
| ページ全長 | 41,037px（800pxの画面で約51枚分） |
| transition が指定された要素 | 817個 / 全7,145要素 |
| 折り返しの下で待機中 | 87個中12個が透明、11個が位置をずらされている |
| video | 13本。全部 muted・controls なし・**autoplay false**（＝JSが再生位置を握っている） |
| canvas | 0（特殊な描画技術は使っていない） |

**動きの見本市を作ってある**（本番サイトには入っていない）

```
C:\Users\林田夏樹\Downloads\motion-demo\index.html
```

全長19,762px、13種類。01と02が上記の気に入っている2つ。03入場／04時間差／
05貼りつけ＋差し替え／06貼りつけ＋変形／07視差／08迫る／09横流れ／10線が描かれる／
11数字が回る／12背景が白に戻る／13マスクで開く。
生成スクリプトは残していない（HTMLを直接書き換えて育てた）。作り直すならHTMLを直接編集する。

**使った絵は旧サイトの素材**。`git log --all --diff-filter=A -- public/image` で消えた13枚を
回収して使っている（SavvyBot・Rotti・HANA・キャラクター類）。**いまのミニマルな
デザインとは方向が違うので、動きの見本にはなるが完成形の雰囲気にはならない。**
採用が決まったら、**各セクションに置く絵を新しく用意する話が必ず出る**。ここが最大の未解決点。

**先に伝えてある副作用**：Apple式は1画面1メッセージでページを長くする方向。
2026-08-12 に余白を詰めてトップを短くしたばかりなので、**この2つは逆を向いている。**

#### 結論：一度本番に作ったが、戻した（2026-08-12・第8セッション）

**本人の判断は「技術的にはよいが、サイトの感じとマッチしない」。全部 revert 済み。**
`app/page.tsx` は元のまま。作ったものは残していない（下記のとおり再現はできる）。

**やめる理由は「動きが作れないから」ではない。** 実装は成立していて、ライト／ダーク、
PC／スマホ、lint／ビルドまで通っていた。**合わなかったのは雰囲気のほう。**
次に同じ話が出たときは、実装から入らずに**まず雰囲気の擦り合わせから始めること。**

**作ったもの（消したが、必要なら再現できる）**

| ファイル | 中身 |
|---|---|
| `app/components/motion/use-scroll-progress.ts` | 貼りつき区間の進捗を 0→1 で返す。state は更新せず DOM を直接触る |
| `app/components/motion/giant-type.tsx` | 01。黒地に巨大タイポ、文字を窓にして中にテクスチャ |
| `app/components/motion/full-bleed-metrics.tsx` | 02。1件1画面。写真が無いので実測値を主役に据えた |
| `app/components/motion/reveal.tsx` | 13 と 03。左から開く／下から現れる |
| `scripts/build-textures.py` | 鍛流線・旋盤の目のテクスチャSVGを生成 |

**この試作で分かったこと（次に同じことをやるなら効く）**

- **文字の窓に入れるテクスチャは「明るい地に暗い線」でないと読めない。** 黒地に白線で作ると、
  黒い画面では文字の中の大半が黒のままになる。一度これで作り直した
- **ダークモードで黒がツートンになる。** サイトの地は `0 0% 8%`（#141414）なので、
  演出側に `#000` を置くと境目に段差が出る。ダークは地の色を使って継ぎ目を消す
- **ページ全長は 2,700px → 10,680px（3.9倍）**になった。Approach だけに絞ると 5,820px。
  増えるのは「スクロールしているだけの区間」だが、数字としては大きい
- 依存ライブラリは要らない。sticky ＋ rAF ＋ IntersectionObserver だけで足りる

**未回答のまま残っている論点**（雰囲気の話を先にするなら、この先の話）

**本人から新たに確定した条件**

- **現場の実際の画像は使えない。生成する必要がある**（会社の資産のため）
- 配分と画像の加工も要る、との認識あり
- 「いまのサイトのスタイルと相反するところが多いが、両立できるか」が本人の問い

**CCから出した提案。1と2は試作で決着した（下記）。3〜5は生かせる**

1. ~~**両立は可能。ぶつかっているのは「密度」だけ。**~~ → **これは外れていた。**
   密度の問題として解いたが、合わなかったのは密度ではなく雰囲気そのものだった。
   装飾・色・書体の思想が近いことと、置いて馴染むことは別だった
2. **適用範囲は本人が「Hero直後から Contact前まで」と指定 → 実装 → 「Approach だけに」→
   実装 → 「サイトの感じと合わないので戻して」。** 範囲を狭めても解決しなかった。
   **範囲の問題ではない**ことが分かったのが、この試作の一番の収穫
3. **01 の窓に必要なのは写真ではなくテクスチャ。** 文字で切り抜かれた面積は細く、被写体が何かは
   読めない。効いているのは明暗の差だけ。生成で狙うのは金属の断面・鍛造肌・ハッチングの粗密といった
   寄りの画。**被写体が要るのは 02（画面いっぱいのモノ）だけ**で、ここが生成の難所
4. **加工はCSS側で吸収する。** モノクロ化＋朱を薄く重ねる処理をCSSで掛ければ、画像を焼き込まずに
   済み、生成し直した画像に差し替えても破綻しない。1枚目で完成させる必要がない
5. **絵を待たず、テクスチャだけで先に枠を組む**（動きと配分を本番で確定させてから絵を流し込む）か

**13 / REVEAL は「絵の調達が要らない」ので採用候補。** 既存の図8枚にそのまま掛けられる。
ただし図の内容を説明する効果は無く、目を引くだけ。

**デモの不具合を4点直した**（`index.html.bak` に元あり）。**本番サイト側の制約ではない**

| 番号 | 症状 | 真の原因 |
|---|---|---|
| 13 / REVEAL | 昨日からずっと空欄 | `clip-path` を監視対象そのものに掛けており、交差比が常に 0.000。`threshold:.15` に永久に届かない |
| 12 / THEME | 何も起きない | 実装が無かった。スクロール進捗で背景と文字色を連続補間するよう追加 |
| 06・10・13 の朱 | 黒に落ちる | `--primary` `--background` がデモの `:root` に未定義。**本番には両方ある** |
| （不要だった） | — | SVGのサイズ指定。計測したら元から 1136×776 で描画されていた |

**13 の罠は本番移植でもそのまま踏む。** `clip-path` は Chrome の交差判定に反映されるため、
**監視する要素とクリップする要素を分ける**こと。

```css
.reveal   > * { clip-path: inset(0 100% 0 0); transition: clip-path 1.2s; }
.reveal.in> * { clip-path: inset(0 0 0 0); }
```

**この調査で本人の時間を大きく使わせた。** ソースを読んで見つけた欠陥を「原因」と断定し、
直した画面を見ずに2回続けて報告したのが原因。**表示に関わる件は CDP で測ってから書くこと。**

#### そのあと Approach だけ作り直した（2026-08-12 夜・同セッション）

**スクロール演出そのものは見送りのまま。** ただし Approach セクションは中身を入れ替えて
**本番に入っている。未コミット。**

**やったこと**

1. **文言を入れ替えた。** 体言止め二文（「新しい技術を、はやく。」「挑む先は、日常の課題。」）
   → 本人の元の自己紹介文を短く整えたリード文へ。**なぜやっているか**が落ちていたため
2. **見出しは置かない。** 一度「欲しい道具は、自分で作る。」を立てたが、
   標語めいて見えると本人判断で外した。**次に見出しを足そうとしないこと**
3. **図を GPT に依頼（H-24）→ 受領 → 差し替え。**
   最初に届いた循環図（現場で見つける→自分で学び、作る→動く道具にして戻す）は
   組み込んだが「いまいち」で不採用。**いま入っているのは2版目の
   `h-24-glowing-sphere-network.svg`**（線画の球面を朱の光が8本、12秒ループで回る）
4. **段組みにした。** 図を本文の下に敷くと本文が左・球が中央で繋がって見えなかったため、
   Hero と同じ「文章が左・図が右」に揃えた

**新しく入ったコード**

| ファイル | 中身 |
|---|---|
| `app/components/scroll-draw.tsx` | 画面に入ってから線が引かれる枠。id の `-lines` / `-fills-and-text` で線と塗りを振り分ける |
| `app/components/diagrams/approach-sphere.tsx` | H-24 の球。`viewBox` を 0 0 1200 680 → 306 81 588 588 に切り詰めてある（中身は無変更） |
| `app/globals.css` | `.scroll-draw` の transition。Hero の `.ds-draw` は読み込み時に走るので折り返しより下では使えない |

**踏んだ罠（次も踏む）**

- **変換スクリプトが `<style>` の中の `url(#…)` に前置きを付けていなかった。**
  参照先の無い filter を指すと**その要素は描画されない**（光が全部消えた）。
  `scripts/svg-to-diagram.py` を直した。既存8枚は無事（全件確認済み）
- **`--sp` 無しで変換すると `minWidth` の横スクロール逃げが入る。**
  文字の無い図では不要。外して縮小させる
- **SMIL（`<animate>`）は `prefers-reduced-motion` を見ない。** CSSでも止まらないので
  `pauseAnimations()` で明示的に止めている
- **時間差は図ごとに数える。** 横組みと縦組みを2枚並べていると、隠れているほうの
  本数まで遅延に足され、スマホで最後の17本が4秒経っても描き終わらなかった

**残っていること**

- ~~未コミット~~ → **2026-08-13 にコミット済み**
- ~~GPT への「H-24 差し戻し」の取り消し~~ → **2026-08-13 に取り消した。**
  `DIAGRAM_BRIEF` から H-24 節を削除し、`_handoff/` の古い依頼ページも消した
  （`build-handoff.py` は「出ている依頼が無い」で終わる正常な状態）
- **About ページの Approach がトップと食い違っている**（トップだけ入れ替えたため）。
  About 側に何を置くかは未決（`DIAGRAM_BRIEF` の「まだ図が無い場所」にも記載）
- 球は止まらず回り続ける。glow（ぼかし）はサイトで唯一の効果。どちらも本人へ伝達済み

### 0-1. 記事末尾の重複整理（図が片づいたので次の候補）

**GPTに制約を全部外して依頼し直し、4記事に8枚を組み込んだ**（2026-08-11〜12・第7セッション）。
**8枚とも横組み（PC）と縦組み（スマホ）の両方が揃っている。** 原案は `claudedocs/received/`。
図の中に見出し・数値・結論まで入った自己完結型で、図の外に凡例を置いていない。

| 記事 | 図 | キー |
|---|---|---|
| 外観検査AI | Fig.01 全品不良判定と実モデルが同じ「検出率100%」で並ぶ | `metric-blindspot` |
| 外観検査AI | Fig.02 画素差の主因は欠陥ではなく位置ずれ | `position-shift` |
| 外観検査AI | Fig.03 入力に掛けると72.4%、出力なら20.8% | `mask-placement` |
| 外観検査AI | Fig.04 別の日の良品100枚が全数、閾値の右へ | `domain-shift` |
| CAD自動化 | Fig.01 19.6時間の7割が手戻り | `rework-breakdown` |
| CAD自動化 | Fig.02 全ゲート合格の図面が加工できない | `parity-gate` |
| 花製作所 | Fig.01 落ちていたのは入口 | `traffic-funnel` |
| work-hub | Fig.01 25秒 → 0.46秒 | `mail-speed` |

**出ている依頼は無い**（`claudedocs/DIAGRAM_BRIEF_2026-08-11.md` の「状況」を見ること）。
次に図が要る場所の候補は同じ文書の末尾に3件書いてある。

**受け取ったSVGは手で写さない。** `python3 scripts/svg-to-diagram.py` で変換する。
SVGの中の `<style>` は文書全体に効き、受け取る図はどれも `.t` `.l` `.h` `.b` のような
短いクラス名を図ごとに違うサイズで持つため、前置きを1つ落とすと他の図が壊れる。
横組みと縦組みは `Plate` を2枚並べて `lg:` で出し分ける（本文幅が864pxになるのが lg 以上で、
それ未満で1200幅の横組みを出すと文字が12pxを割る）。縦組みが未着の図は `minWidth` を
渡すと枠内スクロールへ逃げる。

**組み込み時にCC側で直したのは描画の不具合だけ。構図・配色・文言は触っていない。**

- CAD自動化 Fig.01（横組み）：朱の帯の上の**白抜きが効いていなかった**。原案は `fill` 属性で
  指定していたが、SVGでは class が presentation attribute に勝つため
  `.rw-t{fill:currentColor}` に負ける。クラス（`.rw-ko`）へ移した。
  **縦組みは作り手側が `.inv` クラスで直してきたので無修正**
- 外観検査AI Fig.04（縦組み）：3本のレーン名が1行に並びきらず重なり、右端が切れていた。
  文言そのままで2行に分けた
- 花製作所 Fig.01（横組み）：先頭の「86,000」だけ `text-anchor` が抜けて矢印に触れていた
- work-hub Fig.01（縦組み）：中身が canvas 下端を越えて締めの行に重なっていた。
  canvas を 1280→1380 に伸ばした（中身は無変更）

**作り手に投げ返したほうが早い、という学びが2回出た。** 外観検査AI Fig.04 の朱枠は
こちらで文字を縮めた直後に「要素ごと消した版」が届き、花製作所 Fig.01 の重なりは
こちらが暫定で枠を下へ落とした翌日に canvas ごと作り直した版が届いた。
**描画の不具合は直す前に伝える。**

**受信箱の監視を自動化した**（2026-08-11）。`~/.claude/skills/gpt-handoff/watch-inbox.sh` を
Monitor で回す。手順と注意は gpt-handoff スキルに書いた。**張る前に `TaskList` で確認し、
張ったらテストファイルで発火を確かめること。**

**その前の経緯（2026-08-11 午後）**：第6セッションで入れた3枚を
「分かりにくい・使えない」という判断で**全部外した**。

**外した理由は、CCが作図の制約を積み上げたこと。**
「SVGに文字を入れない」「装置を描かない」「朱は1図につき1つの意味」
「実測どおりの比率で描く」などを規約として課した結果、
安全側に倒れた図しか出てこなくなった。実測どおりの比率を強いたせいで、
見せたい差が小さすぎて見えない図にもなった。

**以後、図の作り方に制約を置かない。** 守るのは2つだけ。

- 数値・事実を捏造しない
- 伏字ルール（社名・製品名・図番・人名・金額・社内パス）

構図・配色・文字の入れ方・モバイルの扱いは作る側に任せる。**よほど駄目なときだけ直す。**
**良し悪しの判定をCCがしない。** 第6セッションでは CC が「基準を通った」と判断した図が
本人には分からない図だった。判定は本人がする。

**図が要る場所の一覧は `claudedocs/DIAGRAM_BRIEF_2026-08-11.md`。**
場所と、使ってよい数字だけが書いてある。作図規約は削除済み。
CAD自動化（Fig.02）と花製作所（Fig.01）は、未確認だった数値を記録から拾って確定させた
（`~/work/pj-claude-fusion/knowledge/verification-ledger.md` /
`~/work/hana/reports/2026-07-04_売上分解レポート.md`）。

**依頼ページの生成**：`python3 scripts/build-handoff.py` →
`_handoff/index.html`（git管理外）。スマホからは report-hub 経由で
`https://node.taile73628.ts.net:8443/NATSUKI-HP/_handoff/index.html`。
依頼が空のときは何も生成せずメッセージを出して終わる（いまはこの状態）。
**依頼文を直すときは md を直して流し直す**（HTMLを直接触らない）。

**仕組み**
- `app/components/schematic.tsx` — 共通枠。`Plate`（SVG1枚を本文幅に収める／`lg:` 出し分け）と
  `Schematic`（FIG番号の枠。`title` は任意。SVG側に見出しがある図では二度出さない）
- `app/components/article-diagrams.tsx` — 登録表。8件登録済み
- `app/components/diagrams/` — 1図1ファイル。`scripts/svg-to-diagram.py` が生成する
- `app/projects/[slug]/page.tsx` の `figure` レンダラ。未登録キーは素の `<figure>` へ

### 0-2. 第6セッションで完了したこと（図以外）

図の件とは別に、次は入って残っている。

- **文字サイズを一段小さくした**。本文18→15px、記事本文18→16px、H1 48→36px、
  ヒーロー72→60px。`CLAUDE.md` の「タイプスケール」節に表がある
- **縦の余白を3段階に整理した**（外観検査AIの記事のみ）。
  まとまりの中 12〜16px / 小ブロック 22〜32px / セクション境目 52〜72px。
  値は `tailwind.config.ts` の `typography.sm` / `base`。**他ページへの展開は未了**
- **スマホでボタンが効かない問題を直した**。`next.config.mjs` の `allowedDevOrigins`。
  詳細は下の「スマホ確認」節
- **記事の数値を記録に合わせた**（外観検査AI・4か所）。この記事の数値は全部照合済み

### 1. 記事末尾の重複を整理する

各プロジェクト記事の末尾の `Outcomes & Results` / `Challenges Overcome` / `Key Learnings` は
本文と重複している。2セクション程度への統合を検討する。
**日本語の本文なのに末尾だけ英語見出し**な点も揃えたい。実体はフロントマターの
`outcomes` / `challenges` / `learnings`。対象は5記事すべて。

### 2. Approach の文言（本人の判断が要る）

- 「見つける・作る・確かめるを現場の中の一人で回すので、あいだに仕様書も翻訳も挟まりません」は
  分業・文書化・協働を否定しているようにも読める、という指摘がある
- 「AIを軸に」が Problem First の主張と衝突しうる、という指摘もある
- **本人の立ち位置の宣言なので、CCが勝手に薄めない**

循環図の撤去でトップ／About の Approach は**見出し＋リード文だけ**になっている。
ここに何を置くかも未決。

### 3. push（ユーザーの明示的な指示を待つこと）

**41コミットが手元に残っている。** push すると Vercel で本番公開される。

---

## 何が変わったか（要約）

### 技術
Next.js 14 → **16.3**、React 18 → **19.2**、Tailwind 3.4 → **4.3**、ESLint 8 → **9**（flat config）。
未使用だった Three.js 関連（MovingLight系・NetworkAnimation 等）と依存5パッケージを撤去。
`next lint` は廃止済みで、`npm run lint` は `eslint .` を実行する。

### デザイン
**ミニマル・エディトリアル**（ユーザー選択）。白/黒基調＋**朱（バーミリオン）**アクセント1色。
罫線でセクションを区切り、カードと背景色ボックスは使わない。
番号・日付・ラベルは `font-mono`（IBM Plex Mono）＋ `tracking-[0.15em]` ＋ `uppercase`。

ヒーローに**金型の組立断面図モチーフ**（`app/components/die-section.tsx`）を置いている。
断面図（ダイ＋パンチ）／端面図／詳細図B／表題欄で構成。
**実在の設計値は使わず、寸法は記号（ø・R・C・H）のみ**。ここは今後も守ること。

### 図解 — 全部やめた（2026-08-11）

**サイトから図を1枚残らず撤去した。** 記事の模式図7枚とトップ／Aboutの循環図（H-08）。
第3〜第5セッションで作り、作り直しを重ねたが、次の水準を越えられなかった。

> **名称をすべて隠した状態で、「何と何を比較しているか／どこが問題か／
> 何が変化したか／結論はどこか」が判別できること。**
> 文字を消すと意味がなくなる図は、模式図ではなくフローチャート。

図の外の説明文を読まないと解読できない状態が、何度直しても残った。
実データ（`~/work/iidzka-inspection/`）に当たって描き直した回でも越えられていない。

**残してあるもの（仕組みだけ）**
- `app/components/schematic.tsx` — `Schematic` / `Callouts` / `Overlay` / `Legend` / `Readout`
- `app/components/article-diagrams.tsx` — 登録表。**中身は空**
- `app/projects/[slug]/page.tsx` の `figure` レンダラ。未登録キーは素の `<figure>` へ

**次に図を作るなら**、まず上の受け入れ基準を通せるか机上で確かめること。
作り方の規約は `CLAUDE.md`「記事本文への模式図の差し込み」に集約した（撤去後に更新済み）。
経緯と、GPTレビューへの回答は `claudedocs/DIAGRAM_REVIEW_RESPONSE_2026-08.md`。
`claudedocs/DIAGRAM_PLAN_2026.md`（候補19件）は記録として残してあるが、**未着手扱いに戻った**。

**撤去に伴って直したところ**
- トップ：Approach は見出し＋リード文だけになった。Projects のリード文から「上の循環を、」を削除
- About：Approach 同上。Current Means のリード文から「上の 02」を削除
- 外観検査AIの記事：図が消えて重複が露出した段落を1つ統合
- **循環図の中にあった 01/02/03 のステーション本文は、図と一緒にサイトから消えている**。
  テキストとして復活させたい場合は書き起こしが要る（`git show` で旧 `work-cycle.tsx` から拾える）

### コンテンツ
Projects は5件。SavvyBot 関連7ファイルは**全削除済み**（サイト内に参照ゼロ）。

| # | slug | 内容 | metric |
|---|---|---|---|
| 01 | `gaikan-kensa-ai` | 外観検査AIの内製開発 | 過検出率 44.2% → 5.8% |
| 02 | `fusion-ai-automation` | CAD操作のAI自動化 | 手戻り率 70% → 15% |
| 03 | `work-hub-tools` | 自分用の開発基盤 | メール表示 25s → 0.46s |
| 04 | `hanaseisakusyo-rebuild` | 作らないという判断 | 再集計 9,047件 |
| 05 | `hobby` | 山と、車と、波 | なし |

接続ノートは13本（`hello-world.mdx` と `next-js-tips.mdx` を削除）。

---

## 守るべき決定事項

### 1. 伏字ルール（会社業務の公開範囲）
ユーザー決定（2026-08-10）。**必ず伏せる**：

- 会社名・自社名、顧客名、取引先・ベンダー名
- 製品名・部品名・ワーク名・図番・機番
- 社内の人名（図面の承認欄含む）
- **金額全般**（予算、発注額、社内相場、原価、売上実額、返金額）
- 材質・硬度の社内標準、クリアランス等の設計ノウハウ数値
- 社内パス（共有サーバー・NAS）、IPアドレス、メールアドレス、SNSアカウント

**書いてよい**：技術手法、アーキテクチャ、評価設計、精度・性能の推移、失敗と対処、学び。
対象物は「冷間鍛造の小型金属部品」、既存システムは「市販のAI検査プラットフォーム」程度に一般化する。

### 2. 記事の書き方
成功譚ではなく、**失敗と、そこで何を変えたか**を軸にする。既存4記事はすべてこの構成。
数値は必ず記録から取る。**推測で数字を作らない**（該当プロジェクトの記録を読むこと）。

### 3. レイアウト規約
`CLAUDE.md` の「レイアウト統一」節に反映済み。ページヘッダーは
モノスペースのオーバーライン（`text-primary`）→ 大タイポH1 → リード文 の順。

### 4. サイトの中心概念（ユーザー決定・2026-08-11）

**「現場で見つけて、技術で形にして、現場で確かめる」**。
ポートフォリオ全体の骨格として扱ってよい。
**この概念を図にしたもの（循環図・H-08）は2026-08-11に撤去した**が、概念自体は生きている。
トップとAboutの見出し・リード文には残っている。

- **「3つの技術を持っている」という見せ方は禁止**。並列に見えた瞬間に主題が壊れる
- 技術ノードの中身は入れ替わる前提で設計する。現在のスタックは「いまの手段」として従属させ、
  将来技術が増えても図が壊れない構造にすること
- Aboutの旧3本柱（金型設計/AI開発/Web開発）は `Current Means`＝循環の02の中身へ位置づけ直した

**改訂（2026-08-11・第4セッション、ユーザー決定）**：循環の骨格は維持しつつ、
「平均的な循環論」との差分＝**自分の色**を明示する方針に変更した。

- 差分の言語化：「AIを軸に、現場にまだ届いていない技術まで自分で取りに行き、
  一人で輪を閉じる（仕様書・翻訳を挟まない）」。現場ファーストは維持し、技術ファーストに見せない
- 02の本文と中心の一文を差し替え、手段の列挙は「**いまの軸 / AI · LLM** ＋ 周辺」の主従表記へ。
  02のみ番号・罫線を朱で強調。トップ／Aboutのリード文も同じメッセージに揃えた
- 経緯と文言の全文は `claudedocs/DIAGRAM_PLAN_2026.md` H-08 の改訂節を参照

### 5. 5プロジェクトを貫く主題（ユーザー決定・2026-08-11）

**「現場の判断を、技術によって再現可能にする」**。
人が見る・考える・判断する・操作する → そのプロセスを理解する → データ化・形式知化する →
AI/Software/CAD等で支援・再現する → 実際の現場で検証する、という共通構造。

**この言葉をサイト上のキャッチコピーとして採用するとは限らない**。
当面は図（L-03 プロジェクト間の関係図）の設計思想としてのみ使う。

---

## 保留事項（次にやること）

### 優先度：高
1. → 冒頭の「★ 第11セッション」の表を見ること。この節より新しい。
2. ~~push していない~~ → **2026-08-14 に全部 push 済み。手元に未プッシュはゼロ。**
   push は Vercel でそのまま本番公開されるので、**次からもユーザーの明示的な指示を待つこと。**

### 優先度：中
3. **`app/sitemap.ts` と `app/robots.ts` の baseUrl が仮のまま**
   （`https://natsuki-portfolio.vercel.app` に「本番環境のURLに変更してください」というコメント付き）。
   実際の公開ドメインが確定したら直す。`metadataBase` も未設定。
4. **接続ノートのslugが不統一**。`connection-note-005` / `-01-ai-nicknames` / `-02-my-measure` /
   `-03-pause` は内容を表していない。ただしこの4本は**既にpush済み＝公開URLが存在する**ため、
   変更するなら旧URLからのリダイレクトが要る。利得が小さいので現状維持と判断した。
5. **公開範囲として本人に一度確認しておきたい記述**（伏字ルールには抵触しないが、判断は本人のもの）。
   - `connection-note-005.mdx`：「私の会社の不良が『なぜ5回』で減らない」など、
     勤め先の方法論への批評。社名・製品名は出ていない。
   - `kagakukan-wall-editing.mdx`：具体的な市名の施設名と、息子を連れて行った話。居住地域が分かる。

### 優先度：低
6. Projects に `featured` フロントマターを導入し、トップ掲載作品を手動選定できるようにする。
7. `claudedocs/` 内の旧SavvyBot記述と `.serena/memories/savvybot_*.md` は履歴として残置している。
   参照する際は「記事は削除済み」である点に注意。

---

## 作業環境のメモ

### dev サーバー
`npm run dev` で http://localhost:3000 。前セッションでバックグラウンド起動したままの可能性がある。
ポートが埋まっていたら `ss -tlnp | grep :300` で確認して kill する
（古いプロセスが残っていて、変更が反映されていないように見える事故が実際に起きた）。

### スマホ確認（Tailscale）で「ボタンが全部効かない」とき — 2026-08-11 に発生

症状：画面は正常に出るのに、**テーマ切替も3点メニューも反応しない**。コンソールにエラーは出ない。
クライアント側の部品が軒並み死ぬので、原因は個々のコンポーネントではなく**JSの取りこぼし**。

原因は2つ重なっていた。**別物なので切り分けること。**

**(1) Next.js が別オリジンからの dev アセットを 403 で弾いていた（修正済み）**

Next は既定で、起動したホスト名（localhost）以外からの dev アセット要求を拒否する。
Tailscale の名前で入ると `@radix-ui/react-icons` などの chunk が **403** になり、
その chunk に載っている操作系だけが死ぬ。`next.config.mjs` の `allowedDevOrigins` で解決。
公式ドキュメントは `node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/allowedDevOrigins.md`。

**(2) WSL2 の localhost 中継が不安定（未解決・本人の判断待ち）**

Tailscale は**Windows側**で動いていて `127.0.0.1:3000` へ転送する。dev サーバーは**WSL2の中**。
この中継が並列接続を取りこぼし、chunk が散発的に **502** になる。

実測（`Input.dispatchMouseEvent` で実際にクリックして開くか数えた）：

| 経路 | 結果 |
|---|---|
| `http://localhost:3000`（WSL内から） | **5 / 5 成功** |
| `https://node.taile73628.ts.net:8445`（Windows経由） | **2 / 5 成功** |

**サイトのコードは正常。** 切り分けの決め手：Windowsから `127.0.0.1:3000` は届くのに、
同じく `*` で待ち受けている `127.0.0.1:3001` は**まったく届かなかった**（PowerShellで確認）。
WSLのIP（`192.168.37.6`）へ直接向ける案は、ドキュメント自体が502になり**逆に悪化**したので却下。

**残っている対策案（`wsl --shutdown` が要るので本人の指示を待つこと）**：
`C:\Users\林田夏樹\.wslconfig` の `[wsl2]` に `networkingMode=mirrored` を足す。
WSLがWindowsのネットワークスタックを共有するようになり、localhost中継の問題自体が消える。
Windows 11 build 26200 / WSL 2.6.1.0 なので対応済み。**実行するとWSLが再起動し、CCのセッションも切れる。**

**当面の回避**：PC確認は `http://localhost:3000` を使う（こちらは安定）。
スマホで操作まで確かめたいときは、うまく動くまで再読み込みするしかない。

### スクリーンショットでの表示確認

**`--window-size` だけのヘッドレス撮影は使わない。** レイアウト幅が反映されず、
「モバイルで横にはみ出している」という**偽陽性**が出る。実際にこれで一度誤診した
（390px指定で撮った画像は本文が右で切れて見えたが、実測の `scrollWidth` は390で問題なし）。

**CDPのデバイスエミュレーションを使うこと。** Node 22 の素の WebSocket で叩ける
（追加パッケージ不要）。手順は次の3つ。

```
Emulation.setDeviceMetricsOverride { width, height, deviceScaleFactor: 2, mobile: width < 700 }
Emulation.setEmulatedMedia        { features: [{name:'prefers-color-scheme', value:'dark'}] }   ← ダーク確認
Page.captureScreenshot            { clip, captureBeyondViewport: true }                          ← 折り返し下も撮れる
```

`Runtime.evaluate` で要素の `getBoundingClientRect()` を取れば、**特定の図だけを切り出して**
撮れる。ページ全体を撮ってから目視で探す必要がない。
横はみ出しの実測も `Runtime.evaluate` で `documentElement.scrollWidth` を読むのが確実。

確認するのは **1440px（PC）と 390px（スマホ）、ライトとダークの両方**。
**塗り（`fill` + `opacity`）はダークで想定より強く出る**ので、範囲を示すときは塗りより線を使う。

### 横方向のはみ出し検査
`components/overflow-debug.tsx` という一時的なデバッグコンポーネントを作って使い、**撤去済み**。
再検査が必要なら、`document.documentElement.scrollWidth` と `clientWidth` を比較して、
`getBoundingClientRect().right > viewport` の要素を列挙する同等品を作り直せばよい。
最終確認時点では 500 / 780 / 1024 / 1280px すべてで `scrollWidth == viewport`。

### アニメーションの注意
`app/globals.css` の `.ds-draw` / `.ds-draw-loop` / `.ds-fade` は `animation-fill-mode: both` を使い、
**既定を「描画済み」の状態にしてある**。`forwards` にすると、アニメーションが走らない環境で
図が消える。ここは意図的なので戻さないこと。

`.ds-draw` の `stroke-dasharray` は 1400。**周長がこれを超える図形に使うと線が途切れる**。
長い輪郭には `.ds-draw-loop`（dasharray 1700）を使う。それも足りなければ新しく足すこと。

---

## 参照すべきファイル

| 用途 | 場所 |
|---|---|
| リニューアルの全経緯・伏字ルール | `claudedocs/RENEWAL_PLAN_2026.md` |
| プロジェクト規約・レイアウト規約 | `CLAUDE.md` |
| 製図モチーフ | `app/components/die-section.tsx` |
| **図をやめた経緯・受け入れ基準** | `claudedocs/DIAGRAM_REVIEW_RESPONSE_2026-08.md` |
| 図解の候補一覧（未着手に戻った） | `claudedocs/DIAGRAM_PLAN_2026.md` |
| 模式図の共通枠 / 登録表（中身は空） | `app/components/schematic.tsx` / `article-diagrams.tsx` |
| 作図の規約・受け入れ基準 | `CLAUDE.md`「記事本文への模式図の差し込み」 |
| OGP画像の生成 | `scripts/build-ogp.py` |
| 記事に入れた動画（元ファイルは transfer-conveyance 側） | `app/components/arm-motion.tsx` / `public/video/` |
| トップページ | `app/page.tsx` |
| 記事の元ネタ（実プロジェクト） | `~/work/iidzka-inspection/`, `~/work/pj-claude-fusion/`, `~/work/project-hub/`, `~/work/hana/` |
