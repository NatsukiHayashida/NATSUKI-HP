# 図解が要る場所（2026-08-11 リセット版）

**これが現行版。** 以前ここにあった長い作図規約は削除した。
規約を積み上げた結果として分かりにくい図しか出てこなくなり、
2026-08-11 にサイトの図を全部外して作り直すことにしたため。

`scripts/build-handoff.py` がこの文書を読んで、1図につき1枚のコピー用HTMLを
`_handoff/` へ書き出す。**依頼文を直すときは HTML ではなくこの md を直して流し直す。**
（`## 共通の前提` と `## H-nn. タイトル` の見出し形式を崩すと生成されない）

---

## 状況（2026-08-11 16:30 時点）

**H-11〜H-18 は全部受領し、8枚ともサイトに入っている。** 4記事に合計8枚。

| 記事 | 図 | 横組み | 縦組み（スマホ） |
|---|---|---|---|
| 外観検査AI | Fig.01 評価指標 | ✅ | ✅ |
| 外観検査AI | Fig.02 位置ずれ | ✅ | **無し** |
| 外観検査AI | Fig.03 マスクの位置 | ✅ | ✅ |
| 外観検査AI | Fig.04 ドメインシフト | ✅ | ✅ |
| CAD自動化 | Fig.01 19.6時間 | ✅ | **無し** |
| CAD自動化 | Fig.02 パリティ | ✅ | **無し** |
| 花製作所 | Fig.01 ファネル | ⚠️ 重なりを暫定で外した | **無し** |
| work-hub | Fig.01 メール | ✅ | **無し** |

**残っている依頼は2種類。**

1. **H-19**：花製作所のファネルの作り直し。**枠が文字に重なっていた**ので、
   こちらで枠を下へ落として重なりだけ外してある。構図が崩れているので作り直したい
2. **H-20〜H-23**：縦組み（スマホ版）が無い4枚

縦組みが無い図は、いま最低幅720pxを確保して枠内を横スクロールさせている。
読めはするが、スクロールしないと全体が見えない。

---

## 共通の前提

個人ポートフォリオサイトに載せる技術記事の**図**を作ってほしい。
サイトは製造業（冷間鍛造の金型設計）のエンジニアのもので、白/黒基調に朱を1色使う
ミニマルな見た目。記事はどれも「失敗と、そこで何を変えたか」を軸に書いてある。

**作り方に制約は置かない。** 構図・配色・文字の入れ方はそちらに任せる。
以前こちらから細かい規約（文字を入れるな・装置を描くな・実測どおりの比率で描け等）を
出していたが、その結果として読めない図しか出てこなくなったので**全部撤回した**。
守ってほしいのは次の2つだけで、これはデザインではなく事実の問題。

- **数値・事実を捏造しない。** 依頼文に書いてある数字だけを使う。
  根拠のない個数・割合・分布の形を描かない
- **会社名・製品名・図番・品番・人名・金額・社内パスは入れない。** 部品は汎用の形でよい

### 出してほしいもの

- **SVG 1ファイル**（インラインSVGのコード）。`viewBox` を必ず付ける
- 図の中に文字を入れてよい。むしろ図だけで結論が読めるほうがよい
- 色は `currentColor`（線・文字）と `hsl(var(--primary))`（朱）と
  `hsl(var(--background))`（白抜き）で書いてくれると、明暗テーマの両方でそのまま動く。
  難しければ普通の色指定でよい。こちらで置き換える
- 説明が要るなら別テキストで添える。無くてもよい

### 実装側で分かったこと（次からこうしてもらえると助かる、程度の話）

- **白抜きは `fill` 属性ではなくクラスで指定してほしい。** SVGでは class のほうが
  presentation attribute に勝つため、`class="t n" fill="hsl(var(--background))"` と書くと
  `.t{fill:currentColor}` に負けて白抜きにならない（H-15 で実際に起きた）
- **canvas に収まるか確認してほしい。** 日本語は想定より横に伸びる。
  H-10 は朱の枠から文字がはみ出し、H-17 は枠がファネルに重なっていた

テーマ対応・id の衝突回避・React化は、組み込むときにこちら側で行う。
**返答は `C:\Users\林田夏樹\Downloads\gpt` に保存すれば拾われる。**

---

## H-19. 花製作所 ファネルの作り直し ― 重なりを直す（横組み＋縦組み）

先に受け取った `h-17-funnel.svg` を組み込んで確認したところ、**重なりがあった**。

- 「サイト内」の枠（x810〜1140）が、ファネル最終段の「EC注文 24」（x1030付近）に被る
- ファネルの横幅970と枠の幅330は、canvas 1200 に横並びで入らない。**横に置く限り必ず重なる**
- こちらで枠を下の空き帯へ落として重なりだけ外した（高さ760→910）。
  ただし枠の左が大きく空いて、左右の対比という構図が崩れている

**canvas の縦横は自由に決めてよい。** 横組み（PC）と縦組み（スマホ）の2本欲しい。

**結論**：ECサイトの全面リプレイスの設計まで進めたが、データを見て**作らないと決めた**。
サイトの中（客単価・カゴ落ち・リピート）はむしろ健全で、
落ちていたのは**サイトに来る前の入口**だった。だから作り直しても効かない。

| 事実 | 値 |
|---|---|
| 分析した注文 | 9,047件 |
| 購入者 | 2,174人（2回以上 617人 / 5回以上 118人） |
| 売上に占めるリピート | 約半分 |
| カゴ落ち率 | むしろ改善 |
| SNSの閲覧 | 86,000回/月（投稿20本） |
| → プロフィールへのアクセス | 1,865/月（閲覧の2.2%） |
| → 外部リンクのタップ | 816/月（プロフ訪問の44%）。**前の期間比 28.5%減** |
| → ECの注文 | 24件/月（タップの約2.9%。この率は安定） |
| 以前の水準を同じ効率で出すのに要るタップ | 月2,400（いまの約3倍） |

- 転換率（2.2% → 44% → 2.9%）はどれも悪くない。**掛け算の一番左が細っている**のが問題
- 店名・商品名・売上実額は出さない

---

## H-20. 外観検査AI 位置ずれ の縦組み（スマホ版）

横組み（`h-14-position-shift.svg`、1200×650）は受け取って組み込み済み。
**同じ結論・同じ数値で、縦長のスマホ版**を作ってほしい。幅720程度・高さ自由。

横組みは同じ部品を2つ重ねた輪を左右に並べ、下に数値を置いている。
スマホでどう畳むかは任せる。

**結論**：良品と不良品の画素の差より、良品同士の位置ずれによる差のほうが大きい。
だから画素の単純比較では原理的に届かない。ここが教師なし異常検知へ切り替えた理由。

| 事実 | 値 |
|---|---|
| 良品同士の平均ピクセル差 | 8.7 |
| 良品と不良品の平均ピクセル差 | 13.2〜14.9 |
| 差の内訳 | 大半は欠陥ではなく位置ずれ |

---

## H-21. CAD自動化 19.6時間の内訳 の縦組み（スマホ版）

横組み（`h-15-rework-breakdown.svg`、1200×700）は受け取って組み込み済み。
**同じ結論・同じ数値で、縦長のスマホ版**を作ってほしい。幅720程度・高さ自由。

**結論**：5日間の作業時間のうち7割が手戻り・是正・復旧だった。
正味の新しい作業は3割しかない。しかも記録の4分の1は、クラッシュや再起動で
計測そのものが失われて概算になっている（＝この7割すら控えめな数字）。

| 事実 | 値 |
|---|---|
| 5日間の作業 | 38件 / 合計19.6時間 |
| 手戻り・是正・復旧 | 13.8時間（70%） |
| 正味の新しい作業 | 5.8時間 |
| 計測が失われて概算になった割合 | 記録の25% |
| このあと下がった手戻り率 | 70% → 15% |

> 朱の帯の上に白抜きの文字を置くときは、`fill` 属性ではなく**クラス**で指定すること
> （横組みでこれが効かず、こちらで直した）。

---

## H-22. CAD自動化 パリティ の縦組み（スマホ版）

横組み（`h-16-parity-gate.svg`、1200×820）は受け取って組み込み済み。
**同じ結論・同じ数値で、縦長のスマホ版**を作ってほしい。幅720程度・高さ自由。

横組みは原本と納品物を左右に並べている。スマホでは上下に積むことになると思うが、任せる。

**結論**：用意した検査ゲートは全部合格していた。しかしその図面では加工ができなかった。
原因は、ゲートが「生成した図面の内部での辻褄」しか見ていなかったこと。
**原本と突き合わせる検査が一つも無かった**。門は、門が見ている対象しか守らない。

| 項目 | 原本 | 納品したもの | 検査していたか |
|---|---|---|---|
| 用紙 | A4 縦 | A3 横 | していない |
| 尺度 | 1:1 | 2:1 | していない |
| 投影図の数 | 3面 | **1面** | していない |
| 品番のリスト | あり | 丸ごと欠落 | していない |
| 一般公差の注記 / 面粗さ / 面取りの指示 | あり | 抜け | していない |
| 寸法・表題欄・図枠の内部整合 | ― | 合格 | **していた** |

- 投影図が1面しかないと、加工する人は二面幅があるのかないのかを確かめる手段がない
- 納品したのは図面3枚。ゲートは全て合格していた
- パリティ照合を入れたあと、説明のつかない差は**1件**（承認欄の氏名）まで減った
- **列挙漏れ＝検査漏れ**。「比べる項目を思いつく」設計だったのが真因

> 品番そのもの・会社名・人名は図に入れないこと。「品番のリスト」までにする。

---

## H-23. work-hub メール表示 の縦組み（スマホ版）

横組み（`h-18-mail-speed.svg`、1200×650）は受け取って組み込み済み。
**同じ結論・同じ数値で、縦長のスマホ版**を作ってほしい。幅720程度・高さ自由。

**結論**：遅かったのは処理そのものではなく、**要らないものまで取っていたこと**。
一覧で全件を取得していたのを必要分だけにし、個別表示も1通だけ取得して10分キャッシュした。

| 事実 | 値 |
|---|---|
| メール表示 | 25秒 → 0.46秒 |
| 個別表示 | 19秒 → 0.36秒 |
| 一覧 | 全件取得 → 必要分だけ取得（＋起動時の先読み） |
| 個別表示の方式 | 1通だけ取得＋10分のキャッシュ |

> **これ以外の数値（件数・回数・バイト数）は確認できていない。** 作らないこと。

---

## 受け渡しの経路

- **受信箱**：`/mnt/c/Users/林田夏樹/Downloads/gpt`
- **監視は Monitor で自動化した**（2026-08-11）。
  `~/.claude/skills/gpt-handoff/watch-inbox.sh` を5秒ポーリングで回し、
  新着・差し替えを1件1行で通知する。`/mnt/c` は inotify が効かないためポーリング必須。
  張ったら**必ずテストファイルを置いて通知が来ることを確かめる**（黙って空回りした前科あり）
- コピー用ページ：`python3 scripts/build-handoff.py` → `_handoff/index.html`
  （report-hub 経由で `https://node.taile73628.ts.net:8443/NATSUKI-HP/_handoff/index.html`）
- 受け取ったSVGは `claudedocs/received/` へ置き、
  `python3 scripts/svg-to-diagram.py` でコンポーネントへ変換する。**手で写さない**
